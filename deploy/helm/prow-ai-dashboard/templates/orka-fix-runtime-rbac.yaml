{{- include "prow-ai-dashboard.validateFixRuntime" . -}}
{{- include "prow-ai-dashboard.validateAnalysisRuntime" . -}}
{{- include "prow-ai-dashboard.validateAI" . -}}
{{- $fixServiceAccount := include "prow-ai-dashboard.orkaServiceAccountName" . -}}
{{- $sourceServiceAccount := include "prow-ai-dashboard.orkaSourceServiceAccountName" . -}}
{{- if and .Values.orka.fixRuntime.enabled .Values.orka.rbac.create }}
# Fix generation keeps its own Task-only Role.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "prow-ai-dashboard.orkaRBACName" . }}
  namespace: {{ .Values.orka.namespace }}
  labels:
    {{- include "prow-ai-dashboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: orka-fix-runtime
rules:
  - apiGroups: ["core.orka.ai"]
    resources: ["tasks"]
    verbs: ["create", "get", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "prow-ai-dashboard.orkaRBACName" . }}
  namespace: {{ .Values.orka.namespace }}
  labels:
    {{- include "prow-ai-dashboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: orka-fix-runtime
subjects:
  - kind: ServiceAccount
    name: {{ $fixServiceAccount }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "prow-ai-dashboard.orkaRBACName" . }}
  apiGroup: rbac.authorization.k8s.io
{{- if and .Values.server.chat.sourceInvestigation.enabled .Values.server.actions.enabled (ne $sourceServiceAccount $fixServiceAccount) }}
---
# The source-facing server can mint only a short-lived, Pod-bound token for the
# exact fix ServiceAccount. It receives no direct fix Task permission.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "prow-ai-dashboard.orkaFixTokenRBACName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "prow-ai-dashboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: orka-fix-token
rules:
  - apiGroups: [""]
    resources: ["serviceaccounts/token"]
    resourceNames: [{{ $fixServiceAccount | quote }}]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "prow-ai-dashboard.orkaFixTokenRBACName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "prow-ai-dashboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: orka-fix-token
subjects:
  - kind: ServiceAccount
    name: {{ $sourceServiceAccount }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "prow-ai-dashboard.orkaFixTokenRBACName" . }}
  apiGroup: rbac.authorization.k8s.io
{{- end }}
{{- end }}
