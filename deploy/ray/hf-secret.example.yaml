apiVersion: v1
kind: Secret
metadata:
  name: hf-token
type: Opaque
stringData:
  # Hugging Face token with access to moonshotai/Kimi-K2-Instruct.
  # Create the real secret instead of committing this:
  #   kubectl create secret generic hf-token --from-literal=hf_token=hf_xxx
  hf_token: <your-hf-access-token>
